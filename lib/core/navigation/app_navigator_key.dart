import 'package:flutter/material.dart';

/// مفتاح عام للـ [Navigator] — يُستخدم لعرض SnackBar للإشعارات في المقدمة (Foreground).
final GlobalKey<NavigatorState> appNavigatorKey = GlobalKey<NavigatorState>();
